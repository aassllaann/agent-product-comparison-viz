flowchart TD
    A[用户输入] --> B[用户交互与展示模块<br/>(UI展示与可视化)]

    B --> C[用户需求理解与语义解析模块<br/>(自然语言 → JSON结构化参数)]
    C --> D[商品评分与推荐模块<br/>(多维评分 + 场景化推荐 + 生态推荐)]

    D --> B
    D --> E[商品数据管理模块<br/>(数据库 + 数据清洗)]
    E --> C
    E --> D

    subgraph F[动态扩展机制]
        direction LR
        E
        D
        C
    end